import { EcoLoader } from "@/components/brand/eco-loader";

// Inside the sidebar for every org page without its own skeleton.
export default function Loading() {
  return <EcoLoader label="Loading page" />;
}
