interface:
  display_name: "CarbonSite"
  short_description: "Repo-specific patterns and workflows for CarbonSite"
  default_prompt: "Use the CarbonSite repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true